;;; -*- lexical-binding: t -*-

(defun childframe-workable-p ()
  "Whether childframe is workable."
  (not (or noninteractive
           emacs-basic-display
           (not (display-graphic-p)))))

(provide 'init-funcs)
