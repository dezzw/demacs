{ lib
, python3
, melpaBuild
, trivialBuild
, fetchFromGitHub
, substituteAll
, git
, writeText
, unstableGitUpdater
}:

let
  rev = "81694390df7172bad8df1d834bf3cddf5634d084";
  python = python3.withPackages (ps: with ps; [
    epc
    orjson
    sexpdata
    six
    inflect
    pyqt6
    pyqt6-sip
  ]);
in
melpaBuild {
  pname = "holo-layer";
  version = "20230818";

  src = fetchFromGitHub {
    owner = "manateelazycat";
    repo = "holo-layer";
    inherit rev;
    hash = "sha256-/HdbE9qerjAON4+Sj1wI0nBmNxhAWhOl2aL20CR1I4Y=";
  };

  commit = rev;

  patches = [
    # Hardcode the python dependencies needed for lsp-bridge, so users
    # don't have to modify their global environment
    (substituteAll {
      src = ./hardcode-dependencies.patch;
      python = python.interpreter;
    })
  ];

  # packageRequires = [
  #   acm
  #   markdown-mode
  #   posframe
  # ];

  checkInputs = [
    git
  ];

  recipe = writeText "recipe" ''
    (holo-layer
      :repo "manateelazycat/holo-layer"
      :fetcher github
      :files
      ("*.el"
       "*.py"
       "plugin"
       "resources"))
  '';

  # doCheck = true;
  # checkPhase = ''
  #   runHook preCheck

  #   cd "$sourceRoot"
  #   mkfifo test.log
  #   cat < test.log &
  #   HOME=$(mktemp -d) python -m test.test

  #   runHook postCheck
  # '';

  passthru.updateScript = unstableGitUpdater { };

   meta = {
    description = "HoloLayer is a multimedia layer plugin specifically designed for Emacs.";
    license = lib.licenses.gpl3;
    platforms = lib.platforms.all;
  };
}
